name: "Code Coverage"

on:
  push:
    branches: [ main ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ main ]

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: [ 'csharp' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Coverlet coverage integration test
      run: dotnet test -c Debug --filter "Category!=Docker" -p:coverletOutput=coverage.xml -p:Threshold=80 -p:ThresholdStat=Total -p:CollectCoverage=true -p:CoverletOutputFormat=opencover -p:ExcludeByAttribute=\"Obsolete,GeneratedCodeAttribute,CompilerGeneratedAttribute\" -p:Exclude=\"[Arcus.WebApi.Tests.*]*\" src/**/*.Tests.Integration.csproj

    - name: Codecov
      uses: codecov/codecov-action@v3.1.1
      if: always()
